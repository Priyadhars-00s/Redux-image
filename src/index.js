import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

//creating the central store with a reducer, thunk, and logger
const store = "";

const rootElement = document.getElementById("root");
ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  rootElement
);
