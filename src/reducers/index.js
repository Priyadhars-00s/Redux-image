//import actions as variables to reduce hard to catch errors from typos
import {} from "../actions/index.js";

//set initial state
const initialState = "";

//define reducer cases for (loading), (got the data), and (didn't get the data)
export const reducer = (state = initialState, action) => {};
